import styled, {css} from 'styled-components';
import { FlexGrid, FlexGridCol, Grid } from '../../commons/mixins';

export const RelatedProductBlock = styled.div`
   
`;

export const RelatedProductFlexGrid = styled.div`
  display: flex;
  margin: 0 0 20px 0;
`;

export const RelatedProductFlexGridCol = styled.div`
  flex: 1;
  background: salmon;
  padding: 20px;
`;

export const RelatedProductGrid = styled.div`
  background: white;
  box-shadow: 0px 3px 19px 0px rgba(0,0,0,0.22);
  border-radius: 10px;
  overflow: hidden;
`;
