import styled, {css} from 'styled-components';

export const FlexGrid = styled.div`
  display: flex;
  margin: 0 0 20px 0;
`;
export const FlexGridCol = styled.div`
  flex: 1;
  background: salmon;
  padding: 20px;
`;

export const Grid = styled.div`
  background: white;
  box-shadow: 0px 3px 19px 0px rgba(0,0,0,0.22);
  border-radius: 10px;
  overflow: hidden;
`;